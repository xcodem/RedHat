GitHub commands


git remote -v to see the 

git remote add RedHat ( or any other meaningful name to you) https://github.com/xcodem/RedHat.git

git push --set-upstream RedHat master   to set the remote repo as default for the push 